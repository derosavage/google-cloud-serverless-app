# Google Cloud Platform Configuration
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
BQ_DATASET=document_pipeline
BQ_TABLE=processed_metadata

# Application Settings
MOCK_GCP=true                       # Set to "false" to use real GCP services
PORT=8080                           # Local development port

# Cloud Run (set by platform at runtime)
# PORT is automatically injected by Cloud Run